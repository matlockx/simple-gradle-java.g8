apply plugin: "java"

version: $version$
group: $group$

def gradleWrapperVersion = "$gradle_version$"

repositories {
    mavenCentral()
}

task wrapper(type: Wrapper) { gradleVersion = gradleWrapperVersion }
